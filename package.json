{
	"name": "loom",
	"private": true,
	"description": "Loom — spatial research canvas. frontend/ = UI + thin Tauri shell, backend/ = all processing behind a local HTTP/SSE API.",
	"scripts": {
		"dev": "cd frontend && npx tauri dev",
		"dev:web": "cd frontend && npm run dev",
		"dev:backend": "cd backend && bun run dev",
		"build": "cd frontend && npx tauri build",
		"test": "cd backend && bun test && cd ../frontend && npm test",
		"test:backend": "cd backend && bun test",
		"test:frontend": "cd frontend && npm test"
	},
	"dependencies": {
		"fallow": "^2.102.0"
	}
}
